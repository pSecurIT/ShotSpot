# ==============================================================================
# FRONTEND LOCAL DEVELOPMENT CONFIGURATION
# ==============================================================================
# This file contains frontend environment variables.
# 
# ⚠️  CONFIGURATION INHERITANCE:
#   All frontend configuration (VITE_API_URL, VITE_NODE_ENV, etc.) is
#   inherited from the root .env file.
# 
#   Only create this file if you need to override specific values for
#   local frontend development.
# 
# For shared configuration, edit the root .env file instead.
# ==============================================================================

# No local overrides needed - all configuration inherited from root .env

# ==============================================================================
# EXAMPLE OVERRIDES (uncomment if needed)
# ==============================================================================
# Override API URL if backend runs on different port locally
# VITE_API_URL=http://localhost:3002/api

# Optional: Analytics or tracking IDs (for future use)
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=
