# ==============================================================================
# BACKEND LOCAL DEVELOPMENT OVERRIDES
# ==============================================================================
# This file contains ONLY local development overrides for backend.
# Most configuration is inherited from the root .env file.
# 
# ⚠️  Only include variables that MUST differ from root .env:
#   - Database connection (localhost vs Docker's 'db')
#   - Local development credentials
#   - Development-specific secrets
#   - Permissive rate limits for local testing
# 
# For shared configuration, edit the root .env file instead.
# ==============================================================================

# Database Configuration (Local Development)
# Override DB_HOST from root .env (which uses 'db' for Docker)
DB_HOST=localhost
DB_USER=shotspot_user
DB_PASSWORD=your_secure_password_here

# Security Secrets (Development)
# ⚠️  Generate strong secrets with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-minimum-32-chars
SESSION_SECRET=your-session-secret-key-minimum-32-chars
CSRF_SECRET=your-csrf-secret-key-minimum-32-chars

# Database Superuser (Local Setup)
POSTGRES_PASSWORD=your_postgres_password

# CORS Configuration (Multiple Local Ports)
# Include all local development URLs
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3001

# Rate Limiting (Permissive for Development)
# Set higher limit for local testing
RATE_LIMIT_MAX=5000

# Twizzit API Integration (Belgian Korfball Federation)
# Generate encryption key with: openssl rand -hex 32
TWIZZIT_ENCRYPTION_KEY=your-64-character-hex-encryption-key-here